<h1>Cocktails</h1>

<% @cocktails.each do |cocktail| %>
  <h3> <strong> - <%= link_to cocktail.name, cocktail_path(cocktail.id) %></strong> ---- <%= link_to "Delete", cocktail_path(cocktail[:id]), method: :delete, data: { confirm: "Are you sure?" } %></h3>
<% end %>

<%= link_to '+ create new cocktail ', new_cocktail_path %>
